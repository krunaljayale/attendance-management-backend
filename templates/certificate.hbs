<!DOCTYPE html>
<html>
<head>
  <style>
    @page {
      size: A4 landscape;
      margin: 0;
    }
    
    body {
      font-family: 'Georgia', serif;
      margin: 0;
      padding: 0;
      height: 100vh;
      width: 100vw;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: #fdfbf7; /* Slight cream color */
      color: #333;
      box-sizing: border-box;
    }

    /* The Border Container */
    .certificate-container {
      border: 10px solid #784705;
      padding: 10px;
      width: 90%;
      height: 85%;
      position: relative;
      background: white;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
    }

    .inner-border {
      border: 2px solid #daa520; /* Gold inner border */
      height: 100%;
      width: 100%;
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 40px;
      position: relative;
    }

    /* Header Section */
    .org-name {
      font-size: 36px;
      font-weight: bold;
      text-transform: uppercase;
      color: #333;
      margin-top: -20px;
      letter-spacing: 2px;
    }

    .header { 
      font-size: 50px; 
      font-weight: bold; 
      color: #784705; 
      font-family: 'Times New Roman', serif;
      margin: 10px 0;
    }

    .sub-header { 
      font-size: 22px; 
      color: #555;
      font-style: italic;
    }

    /* Student Name */
    .student-name { 
      font-size: 42px; 
      font-weight: bold; 
      border-bottom: 2px solid #daa520;
      padding: 0 20px;
      margin: 20px 0;
      color: #222;
      text-transform: capitalize;
    }

    /* Content Body */
    .content { 
      font-size: 18px; 
      line-height: 1.8; 
      color: #444; 
      max-width: 80%;
      text-align: center;
    }
    
    .bold-text {
      font-weight: bold;
      font-size: 22px;
      color: #000;
    }

    /* Footer Section */
    .footer {
      width: 100%;
      display: flex;
      justify-content: space-between;
      margin-top: 60px;
      padding: 0 50px;
    }

    .signature-block {
      text-align: center;
    }

    .signature-line {
      border-top: 2px solid #333;
      width: 200px;
      margin-bottom: 5px;
    }

    .signature-text {
      font-size: 16px;
      font-weight: bold;
    }

    /* Verification ID */
    .cert-id {
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 12px;
      color: #999;
      font-family: sans-serif;
    }
  </style>
</head>
<body>

  <div class="certificate-container">
    <div class="inner-border">
      
      <div class="org-name">YOUR INSTITUTE NAME</div>

      <div class="header">Certificate of Completion</div>
      <div class="sub-header">This is to certify that</div>
      
      <div class="student-name">{{name}}</div>
      
      <div class="content">
        has successfully completed the course <br>
        <span class="bold-text">{{course}}</span> <br>
        from <strong>{{startDate}}</strong> to <strong>{{endDate}}</strong> <br>
        securing a grade of <span class="bold-text">{{grade}}</span>.
      </div>

      <div class="footer">
        <div class="signature-block">
          <div class="signature-line"></div>
          <div class="signature-text">Date: {{date}}</div>
        </div>

        <div class="signature-block">
          <div class="signature-line"></div>
          <div class="signature-text">Director Signature</div>
        </div>
      </div>

      <div class="cert-id">Certificate ID: {{certificateId}}</div>

    </div>
  </div>

</body>
</html>